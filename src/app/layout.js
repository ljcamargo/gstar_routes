import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata = {
  title: "G*Routes - CDMX Public Transport AI Finder",
  description: "AI-powered route finder for Mexico City public transport. Optimized with Gemini for human-like reasoning.",
  icons: {
    icon: "/icons/gstar.svg",
    apple: "/icons/gstar.png",
  },
};

export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
